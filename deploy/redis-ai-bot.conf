# Apply these settings to /etc/redis/redis.conf on the ECS host.
bind 127.0.0.1 ::1
protected-mode yes
port 6379

# 2-core/8-GiB profile. Raise this to 1gb or 2gb after an ECS upgrade.
maxmemory 512mb
maxmemory-policy noeviction

appendonly yes
appendfsync everysec
